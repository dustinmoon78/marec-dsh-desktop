# 发布记录 — 0.0.1-rc.12（2026-08-16-1930）

> 依据：AGENTS.md §5 + docs/PR6-兼容优化落地方案-2026-08-16-1815.md（v4，三轮审查收敛）。

## 发布内容

- **PR #6（dustinmoon78）**：背景图功能（设置卡选择/持久化/静态路由 /api/dsh-hub/backgrounds/*）+ DPI 尺寸单位修复（webviewjs logical）+ schema 默认值（热加载）。
- **落地修复（三轮审查收敛）**：
  - 背景图渲染层 blocker：body 注入不可见（AppFrame 不透明覆盖）→ 改 frame 层双层背景（锚点 \#root div[style*="grid-template-columns"]\ + linear-gradient 蒙层 + 图片）；冒烟实测锚点唯一命中 + 注入可见。
  - backgrounds-api.ts 拆分（decodeURIComponent 兜底 404）；config-api 白名单 128→64；title 默认统一 'DeepSeek Harness Hub'；onPickBackground 补 setSaving(true)。
  - 文档：AGENTS 豁免条款（根/client/services/skins）、README、踩坑记录 #32、三处一致核对。

## 执行过程

| 步骤 | 结果 |
|---|---|
| approve + merge PR #6 | 4951df3（dev-v1） |
| 落地修复 + 文档 | 7dc5acd |
| 冒烟（CLI dsh web 3080 + Playwright） | 锚点唯一命中 frame（1280x720 grid）；注入后 computed background-image 双层背景生效 |
| PR #7（dev-v1→main）admin merge | 7d66b09 |
| main 上 npm version 0.0.1-rc.12 | 3b8e001 + tag v0.0.1-rc.12 |
| main 上 build + build:client（§5 重建） | 通过 |
| npm publish --tag rc --registry=npmjs | + @marecgents/dsh-hub@0.0.1-rc.12 |
| dist-tag latest | latest = rc = 0.0.1-rc.12（registry API 直查） |
| push main + tag；merge 回 dev-v1；push dev-v1 | main = 3b8e001；dev-v1 = 3b8e001 |

## 最终状态

- npmjs：0.0.1-rc.12（latest/rc）
- main = dev-v1 = 3b8e001；tag v0.0.1-rc.12 已推送
- 遗留：背景图蒙层透明度 0.25 待用户实机按对比度微调；#root 锚点升级冒烟项（踩坑 #32）
