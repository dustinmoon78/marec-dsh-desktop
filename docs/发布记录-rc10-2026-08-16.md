# 发布记录：v0.0.1-rc.10（2026-08-16）

## 版本

- 前置：`0.0.1-rc.9`（npm `latest` 与 `rc` 均为 rc.9）
- 本次：**`0.0.1-rc.10`**（npm `rc` tag；`latest` 保持 rc.9，预览版不覆盖 latest）

## 本次 RC 携带的内容（相对 rc.9）

| 提交 | 内容 |
|---|---|
| `44b6df1` | 进程身份：`dsh-hub.exe`（DeepSeek Harness Hub）+ `dsh-hub-guard.exe`（Launcher）——rcedit 补丁 node.exe，任务管理器不再显示 "Node.js JavaScript Runtime"；提示音替换为原创合成 4 段（移除 Reasonix/mixkit 素材） |
| `2c8d02f` | 皮肤全面覆盖（sidebar/nav/module/menu token）+ Setting-Cell 皮肤选择器 + 保存回放修复 + 重命名（DSH HUB 设置 / DeepSeek Harness Hub） |
| 此前 | 事件提示音体系（声音+toast 聚焦策略）、PR #2/#3 合并、5 套皮肤、多实例防护、右侧栏等 |

## 执行过程

1. 确认工作区干净、npm 最新 rc = rc.9
2. `npm version 0.0.1-rc.10`（commit `33fbf3c` + tag `v0.0.1-rc.10`）
3. `npm run build` + `npm run build:client` 通过
4. `npm publish --access public --tag rc --registry=https://registry.npmjs.org/` → `@marecgents/dsh-hub@0.0.1-rc.10`
5. 验证：dist-tags `{"rc":"0.0.1-rc.10","latest":"0.0.1-rc.9"}`；tarball 可下载
6. `git push origin dev-v1 --tags`（`44b6df1..33fbf3c` + 新 tag）

## 安装

```sh
npm install -g @marecgents/dsh-hub@rc   # 或 @0.0.1-rc.10
```

## 遗留

- 正式版（latest）仍需 Tauri 2.x 迁移完成后发布（AGENTS.md 版本策略）
