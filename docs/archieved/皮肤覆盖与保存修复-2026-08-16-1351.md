# 皮肤全面覆盖 + 保存修复 + 选择器重设计 + 重命名（2026-08-16 13:51）

> 任务日志（default 工作流收尾归档）。涉及 dsh-hub（deepseek-harness-hub）dev-v1。

## 任务概述

grill 五问确认后执行四项工作：① 皮肤全面覆盖左/中/右 + 卡片 + 文字高对比（frontend-design 系统性美化，保持 PR 风格）；② 勾选保存修复 + 复查；③ 皮肤选择器方案 A（Setting-Cell 行式）；④ 重命名（设置卡片标题 + 窗口标题）；⑤ 文档同步（skins harness + 总 harness + 色板 + 踩坑记录）。

## 关键发现与决策

| 项 | 结论 |
|---|---|
| 勾选丢失根因 | 运行实例 10:43 启动、内存旧代码（无皮肤/声音/新白名单）→ 保存响应缺字段 → 客户端 `setDraft(saved)` 静默回退；用户重启后正常。**防御修复**：`setDraft({...saved, ...patch})` 回放提交，永不静默回退 |
| 皮肤覆盖缺口 | dsh 左导航用 `--dsw-specific-sidebar-fill`、卡片徽章用 `bg-module-platform`、浮层用 `--dsw-specific-menu` —— alias 覆盖不到 → 浅色下左右停留官方白 |
| 解决方案 | `DshSkin.specific {light,dark}` 输出 `--dsw-specific-*` 前缀；5 套皮肤补齐 sidebar-fill / nav-item-* / menu + bg-module-platform，按官方深度规律（sidebar 比 base 走一步、module 再走一步、menu=layer-3）+ 各皮肤品牌色 accent |
| 重命名 | 卡片标题 → DSH HUB 设置；窗口/托盘/toast 标题 → DeepSeek Harness Hub（cordis.patch.yml）；launcher 共存提示引用同步；**AUMID `DeepSeekHarness.Desktop` 保留**（任务栏稳定身份） |
| 选择器 | 方案 A：官方 Setting-Cell 行（label 左 + Menu 药丸右 + 动态描述小字），替代溢出/边框跳动的矩形框网格 |

## 产出文件

- `src/client/skins.ts` — DshSkin.specific 结构 + buildCss 双前缀 + 5 皮肤新 token
- `src/client/settings-card.tsx` — 保存回放修复 + Setting-Cell 皮肤选择器 + 标题
- `src/client/style.ts` — fieldRow / selectPill 类
- `cordis.patch.yml` — 窗口标题；`bin/launcher.mjs` — 卡片名引用
- 文档：`docs/skins/AGENTS.md`（新 skins harness）、5×`docs/skins/*.md`（补 token 表）、根/客户端 `AGENTS.md`、`docs/关键踩坑记录.md` #25/#26
- 提交：`2c8d02f`（dev-v1，已推送）

## 验证

- host tsc + client 构建通过（client 235.05 kB）
- 构建产物抽查：5 套皮肤 bg-module-platform + specific 全套 token 均输出
- `--dsw-specific-*` 前缀由 `block()` 第二参正确生成

## 遗留

- 需重启桌面壳后肉眼验收：浅/深两套下左导航/右详情/卡片/浮层是否一致跟随；皮肤选择器下拉交互
- 启动器共存提示里独立的 "DeepSeek Harness Desktop 已在运行" 文案按用户"只改文本（卡片+窗口标题）"要求未动，如需一致可后续改
