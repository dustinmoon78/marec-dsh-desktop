# PR #6 可行性分析审查报告（2026-08-16-1815 · v4 定稿）

> 修订记录：v1 → v2（第 1 轮：blocker 可见性、merge-base、P1 降级）→ v3（第 2 轮：方案 A 判死、锚点、E2 措辞、'none' 撤销）→ v4（第 3 轮总收敛：`#root > div` 判死、主锚点定稿 `#root div[style*="grid-template-columns"]`、蒙层改双层背景）。
> **结论（v4）：三轮审查收敛完成。commit 2/3 直接 approve；commit 1（背景图）渲染层 blocker 的修复方案已收敛定稿（frame 层注入 + 双层背景蒙层），修复后即可 approve 落地。**

---

## 〇、关键事实（四轮核实定稿）

| # | 事实 | 证据 | 状态 |
|---|---|---|---|
| F1 | merge-base = **c02f641**；PR 缺 rc.11 3 个发布类提交；`git merge-tree` exit 0 无冲突 | 实测 | ✅ |
| F2 | 背景图注入 body **不可见**：`.frame{background:var(--dsw-alias-bg-base);height:100%}` 全窗不透明覆盖 | 实测 | ✅ |
| F3 | **`#root > div` 判死**：settled 后 `#root` 直接子元素是 slot 出口 `div[data-slot="root"][style="display:contents"]`，**无盒**（display:contents 不产生盒）→ 注入背景不可见（红色样张实证 frame 区域无红色）；frame 是其孙节点 | 活体 dsh web 实测 + web-react/scoped-slots.tsx L652/676/872 | ✅ v4 修正 |
| F4 | **主锚点定稿：`#root div[style*="grid-template-columns"]`** —— 实测唯一命中 frame（内联 `grid-template-columns` 序列化，无 hash）；备选可加 `#root div[data-slot="root"] > div`（结构路径，需冒烟） | 活体实测 | ✅ v4 |
| F5 | 方案 A（负 z-index 伪元素）判死：frame `position:relative;z-index:auto` 不建层叠上下文，负 z-index 层确定性在 frame 背景之下（CSS2.1 Appendix E） | CSS 规范 + 实测 | ✅ |
| F6 | webserver 兜底：handler 异常 → `handle().catch → warn + 400`，不崩进程 | webserver/src/index.ts | ✅ |
| F7 | webviewjs `logical` 语义 = 逻辑像素（默认物理）；`kind:'prefix'` 官方支持 | js-bindings.d.ts + docs + host-webserver | ✅ |
| F8 | 静态路径解析包根 assets/backgrounds；files 含 assets；boat.jpg 292KB | 实测 | ✅ |
| F9 | 皮肤 default 与背景 'none' 行为一致（均 `textContent=''`）→ v1 'none' nit 撤销 | skins.ts 实测 | ✅ |

---

## 一、兼容性结论（不变）

- 合并 CLEAN（F1），语义零回退；合并后必须重建 lib（map 陈旧）。
- commit 1：配置/路由/持久化/竞态全部合格，唯一 blocker = 渲染层（F2/F3/F4，修复见落地方案）。
- commit 2：DPI 单位正确（F7），三路径一致；minor：onPickBackground 补 setSaving(true)。
- commit 3：热加载可行；minor：title 统一 + 契约文档化。
- 其余 minor：decodeURIComponent try/catch（F6 兜底 400 → 语义 404）；白名单 64；豁免条款。

---

## 二、审查结论（v4）

三轮审查收敛：无 blocker 级新发现；残留风险仅"锚点依赖 dsh 内联样式（升级漂移 → 背景静默失效，已有冒烟项兜底，可接受）"。**达到可执行标准**。
