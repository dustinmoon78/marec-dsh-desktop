# dsh-hub 三轮回合深度审查报告（2026-08-16）

> 基线：HEAD `6b489f2`（dev-v1）。3 路并行子代理审查（host/进程配置 · client 运行时状态机 · UI/样式/跨版本），关键项人工复核确认。本报告为后续修复的依据与快照。

## A. 已确认必现（6 项）

| # | 严重 | 问题 | 位置 |
|---|---|---|---|
| A1 | 高 | pin 图标裁剪：Material 24 栅格路径配 `viewBox="0 0 16 16"`，右/下 20-30% 被切 | pin-conversations.ts:50-52 |
| A2 | 高 | 终端绿/极光紫深色 `tooltip-bg` 浅色（#a9f0a9/#e2dcff）+ 官方白字 → 不可读 | skins.ts |
| A3 | 高 | `dsh-hub` 终端命令绕过多实例门控（无 detectRunningDshInstances + 确认） | dsh-hub.mjs |
| A4 | 中 | 保存窗口宽高重启不生效（effectiveConfig 恒 undefined、hasStoredWindowSize 死代码） | index.ts/config-api.ts |
| A5 | 中 | 搜索无结果时置顶区闪现（detectSearch 0 行误判） | pin-conversations.ts |
| A6 | 低 | 右侧栏 Git 分支名当 commit hash 显示 | workspace-api/right-sidebar |

## B. 已确认竞态/逻辑 bug（8 项）

| # | 严重 | 问题 |
|---|---|---|
| B1 | 中 | dirtyDelta 复活：boot 合并 `added` 未过滤 `removed`（先置顶再取消 X → X 复活） |
| B2 | 中 | 内容匹配跨文本误标：行内叶子文本 == 他会话标题（状态/时间文案碰撞）→ pin 挂错会话 |
| B3 | 中 | 空 ready 快照清空 pins.json（连续两次空 byId → 全剪并落盘） |
| B4 | 中 | 多标签/多实例置顶数据丢失（无 storage 监听、boot 后不重拉、全量 PUT 最后写者胜） |
| B5 | 中 | 同标签连续 PUT 乱序覆盖（多连接下"顺序保序"假设不成立） |
| B6 | 中 | `mg:shell-command` 监听无 disposer（HMR 后托盘新任务双触发） |
| B7 | 中 | 并发保存竞态（保存/切皮肤并发响应乱序覆盖草稿；host 读-改-写丢字段） |
| B8 | 低 | boot 皮肤恢复竞态（慢 GET applySkin 晚于手动切皮肤 → 闪回） |

## C. 进程/身份（4 项）

| # | 严重 | 问题 |
|---|---|---|
| C1 | 高 | hub-exe cacheKey 取 process.execPath 自指：第 2 次启动起 execPath=guard 副本（rcedit 改过 stat）→ stamp 永不匹配 → 每启重建 + rmSync 运行中 guard 抛错 → 直启路径静默回退 node.exe |
| C2 | 高 | guard spawn 失败 = 静默退出（relaunchAsGuard 返回 true 但 child 没起来） |
| C3 | 中 | 单实例锁 TOCTOU（读-写非原子，并发双启动） |
| C4 | 中 | 多实例检测只匹配 node.exe（dsh-hub.exe 实例不可见 → 共存确认被跳过） |

## D. 未来升级风险（4 项重点）

| # | 严重 | 问题 |
|---|---|---|
| D1 | 高 | 皮肤被"内联 token"静默全灭（theme-presenter 已具备内联能力，未来版本触发） |
| D2 | 高 | 锚点契约漂移（role=tree 即滚动容器/搜索行=button/叶子文本==标题） |
| D3 | 中 | 皮肤 token 覆盖仅官方全集约 1/3（气泡/selector/tip 等漏色） |
| D4 | 中 | 置顶区几何与官方数值耦合（scrollbar 8+2、行高 32px 硬编码镜像） |

## 修复依据备注

- 三轮完整发现（51 项原始）含 12 项低危（死代码/命名/文档），随修复一并清理或记录。
- 关键核验：A1/B1/C1/A2 已人工 grep 复核确认属实。
