# 发布记录 — 0.0.1-rc.11（2026-08-16-1731）

> 依据：AGENTS.md §5 发布流程（dev-v1 开发 → 合并 main 发布 → 构建验证 → 显式 npmjs registry）。

## 发布内容

rc.11 = 漏洞修复批次全部落地（审查报告 → 修复方案 → 执行 → 修复后审查）：
- **client**（9af66c2）：置顶会话状态机加固（A1/A2/A5/B1-B5）、皮肤修复（D3/D4）、设置卡保存竞态（B6-B8）
- **bin**（a4bd427）：多实例门控共享化 + 原子锁（A3/C3/C4）、hub-exe 缓存/relaunch（C1/C2）、tray-helper/裸名清理
- **host**（3b59f78）：窗口尺寸重启生效（A4）、Git 分支语义（A6）、文档（D1/D2）
- **minor 修复**（c02f641）：lock 回读校验 / B2 守卫改比标题 / 默认尺寸污染
- **构建产物**（9cb3ca9）：lib/ 刷新（sourcemap 行尾）

## 执行过程

| 步骤 | 结果 |
|---|---|
| `npm version 0.0.1-rc.11`（dev-v1） | commit 00ab09e + tag v0.0.1-rc.11 |
| `git merge dev-v1 --ff-only` → main | 本地 main fast-forward 到 00ab09e |
| main 上 `npm run build && npm run build:client` | 通过（发布前构建验证） |
| `npm publish --access public --tag rc --registry=https://registry.npmjs.org/` | 成功，+ @marecgents/dsh-hub@0.0.1-rc.11 |
| `npm dist-tag add ...@0.0.1-rc.11 latest` | latest = rc = 0.0.1-rc.11（registry API 直查确认；npm view 本地缓存显示旧值，勿信） |
| push tag v0.0.1-rc.11 + dev-v1 | 成功（dev-v1 = 9cb3ca9） |
| push main | **被拒（GH006）**：main 受保护，必须走 PR |
| gh pr create（dev-v1 → main）+ gh pr merge | PR #5 已建；合入被策略挡（需 ≥1 个 write 权限 review；owner 无法自审） |
| 询问用户 | **用户选择「暂不同步 main」**——main 保持 da83411，PR #5 留待以后 approve 合入 |

## 最终状态

- **npmjs**：`@marecgents/dsh-hub@0.0.1-rc.11`，dist-tags `latest` = `rc` = `0.0.1-rc.11`（与 rc.10 一致：latest 跟随最新 rc）
- **远端**：dev-v1 = 9cb3ca9（含全部 rc.11 提交）；main = da83411（未动，按用户选择）；tag `v0.0.1-rc.11` 已推送
- **PR #5**：open，dev-v1 → main（以后 approve 即可合入，内容与发布构建完全一致）

## 遗留

- main 分支同步待办：approve PR #5 后合入（管理员合入仍受 review 规则约束，需外部 review）。
- 本地 main 分支引用指向 9cb3ca9（领先远端，未推送，无副作用）。
